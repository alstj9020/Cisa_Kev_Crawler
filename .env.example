# 이 파일을 .env 로 복사해서 사용하세요:  cp .env.example .env

# NVD API 키 (선택)
#  - 미설정 시 건당 6초 sleep → 30건 기준 약 3분 소요
#  - 설정 시 건당 0.6초 → 약 18초 소요
#  - 발급: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=

# 크롤 결과 JSON 을 저장할 디렉터리
OUTPUT_DIR=./output

# HTTP 요청 타임아웃 (초)
REQUEST_TIMEOUT=30
